## 简介

防止专用服务器上的第三方地图脚本污染，避免地图 B 的全局脚本在地图 A 中被错误执行。

需要原生键值函数库前置。只有与地图 `mission` 文件位于同一 VPK 内的脚本才会被识别为地图脚本，独立脚本类 Mod 不会被拦截。

## 可用指令

- `sm_vpklist`：打开 VPK 白名单/黑名单管理菜单（Root 管理员）。
- `sm_vpkmenu`：同上（Root 管理员）。
