## 简介

基于导航网格逃生路线的路径指引插件。玩家可通过指令显示通往目标方向的光束路径，也支持长按 TAB 触发提示和持续刷新模式。
> 需要left4hooks版本高于1.166

## 可用指令

- `guide` / `ptg` / `path_to_goal` 显示路径指引
- `pathtogoal` / `wheretogo` / `imlost` 显示路径指引
- `gp_cont` 开关持续路径刷新
- `l4d_path_to_goal_recalculate` 重新计算路径缓存（管理员）

